'use strict';

/**
 * le-bureau-executif router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::le-bureau-executif.le-bureau-executif');
